import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import { ThemeProvider } from 'next-themes'
import { ModelProvider } from '@/lib/model-context'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Vibe Coding - AI-Powered Website Generator',
  description: 'Generate complete Next.js websites from natural language prompts using advanced AI',
  generator: 'v0.app',
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
  openGraph: {
    title: 'Vibe Coding - AI-Powered Website Generator',
    description: 'Generate complete Next.js websites from natural language prompts',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="font-sans antialiased dark">
        <ThemeProvider attribute="class" defaultTheme="dark" enableSystem>
          <ModelProvider>
            {children}
            <Analytics />
          </ModelProvider>
        </ThemeProvider>
      </body>
    </html>
  )
}
